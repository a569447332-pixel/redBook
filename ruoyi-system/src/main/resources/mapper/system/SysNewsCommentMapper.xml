<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.SysNewsCommentMapper">
    <resultMap type="SysNewsComment" id="SysNewsCommentResult">
        <id     property="commentId"    column="comment_id"    />
        <result property="newsId"       column="news_id"       />
        <result property="userId"       column="user_id"       />
        <result property="userName"     column="user_name"     />
        <result property="content"      column="content"       />
        <result property="parentId"     column="parent_id"     />
        <result property="likeCount"    column="like_count"    />
        <result property="status"       column="status"        />
        <result property="createBy"     column="create_by"     />
        <result property="createTime"   column="create_time"   />
        <result property="updateBy"     column="update_by"     />
        <result property="updateTime"   column="update_time"   />
        <result property="remark"       column="remark"        />
    </resultMap>

    <sql id="selectSysNewsCommentVo">
        select comment_id, news_id, user_id, user_name, content, parent_id, like_count, status, create_by, create_time, update_by, update_time, remark
        from sys_news_comment
    </sql>

    <!-- 新增评论 -->
    <insert id="insertSysNewsComment" parameterType="SysNewsComment" useGeneratedKeys="true" keyProperty="commentId">
        insert into sys_news_comment
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="newsId != null">news_id,</if>
            <if test="userId != null">user_id,</if>
            <if test="userName != null">user_name,</if>
            <if test="content != null">content,</if>
            <if test="parentId != null">parent_id,</if>
            <if test="likeCount != null">like_count,</if>
            <if test="status != null">status,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="remark != null">remark,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="newsId != null">#{newsId},</if>
            <if test="userId != null">#{userId},</if>
            <if test="userName != null">#{userName},</if>
            <if test="content != null">#{content},</if>
            <if test="parentId != null">#{parentId},</if>
            <if test="likeCount != null">#{likeCount},</if>
            <if test="status != null">#{status},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="remark != null">#{remark},</if>
        </trim>
    </insert>

    <!-- 按新闻ID查评论 -->
    <select id="selectSysNewsCommentByNewsId" parameterType="Long" resultMap="SysNewsCommentResult">
        <include refid="selectSysNewsCommentVo"/>
        where news_id = #{newsId} and status = '0'
        order by create_time desc
    </select>

    <!-- 修改点赞数 -->
    <update id="updateSysNewsCommentLikeCount">
        update sys_news_comment
        set like_count = #{likeCount}
        where comment_id = #{commentId}
    </update>

    <!-- 修改状态 -->
    <update id="updateSysNewsCommentStatus">
        update sys_news_comment
        set status = #{status}
        where comment_id = #{commentId}
    </update>

    <!-- 通过评论ID查询单条评论 -->
    <select id="selectSysNewsCommentByCommentId" parameterType="Long" resultMap="SysNewsCommentResult">
        <include refid="selectSysNewsCommentVo"/>
        where comment_id = #{commentId}
    </select>






</mapper>
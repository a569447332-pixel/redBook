<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.AppMemberMapper">

    <resultMap type="com.ruoyi.system.domain.AppMember" id="AppMemberResult">
        <id property="memberId" column="member_id" />
        <result property="email" column="email" />
        <result property="nickName" column="nick_name" />
        <result property="avatar" column="avatar" />
        <result property="status" column="status" />
        <result property="createBy" column="create_by" />
        <result property="createTime" column="create_time" />
        <result property="updateBy" column="update_by" />
        <result property="updateTime" column="update_time" />
        <result property="remark" column="remark" />
    </resultMap>

    <select id="selectMemberByEmail" resultMap="AppMemberResult">
        select * from app_member where email = #{email} and del_flag = '0'
    </select>

    <insert id="insertMember" parameterType="com.ruoyi.system.domain.AppMember" useGeneratedKeys="true" keyProperty="memberId">
        insert into app_member (email, nick_name, avatar, status, create_by, create_time, remark)
        values (#{email}, #{nickName}, #{avatar}, #{status}, #{createBy}, #{createTime}, #{remark})
    </insert>

    <select id="selectMemberById" parameterType="Long" resultType="AppMember">
    SELECT * FROM app_member WHERE member_id = #{memberId}
</select>
</mapper>